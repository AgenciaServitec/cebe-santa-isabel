---
import { programs } from "../data-list/";
import ContentWidth from "../components/ContentWidth.astro";
import Card from "../components/Card.astro";
---

<section>
  <ContentWidth>
    <div>
      <div class="flex flex-col md:flex-row py-10">
        <div class="basis-3/7 items-start">
          <h1 class="text-6xl font-semibold text-black">Nuestros Programas</h1>
        </div>

        <div class="basis-1/7 mx-auto items-end pt-15">
          <img class="h-20" src="/images/flecha.png" alt="" />
        </div>

        <div class="basis-3/7 justify-start">
          <p class="text-sm text-gray-600">
            Fomentamos una formación integral con enfoque académico, en un
            ambiente inclusivo y democrático, promoviendo relaciones solidarias;
            con el objetivo de que nuestros egresados sean ciudadanos activos en
            el desarrollo de su comunidad desarrollo de su comunidad y del país.
          </p>
        </div>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-x-2 gap-y-4 md:gap-y-8">
        {
          programs.map((item) => (
            <Card
              title={item.title}
              description={item.description}
              iconName={item.iconName}
              size="xs"
              descriptionSize="xs"
            />
          ))
        }
      </div>

    </div>
  </ContentWidth>
</section>
